import { DadosPessoaisPipe } from './dados-pessoais.pipe';

describe('DadosPessoaisPipe', () => {
  it('create an instance', () => {
    const pipe = new DadosPessoaisPipe();
    expect(pipe).toBeTruthy();
  });
});
